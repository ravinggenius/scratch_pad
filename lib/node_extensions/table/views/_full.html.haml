%header
  %span.h= node.title
%section
  %table
    %caption= filter node.caption
    %thead
    %tfoot
    %tbody
      - node.data.each do |row|
        %tr
          - row.each do |cell|
            %td= filter cell
