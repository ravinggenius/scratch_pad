- is_featured ||= false
- is_single ||= false
- view_to_show ||= (is_featured || is_single) ? :full : :preview

%article.node{ :class => [node.machine_name, view_to_show, ('featured' if is_featured)] }
  = show_node node, view_to_show
