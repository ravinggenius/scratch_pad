!!! 5
%html{ :lang => 'en', :xmlns => 'http://www.w3.org/1999/xhtml', 'xml:lang' => 'en'}
  %head
    %title= @title

    %meta{ 'http-equiv' => 'Content-Type', 'content' => 'application/xhtml+xml; charset=utf-8' }/
    = csrf_meta_tag

    /%link{ :rel => 'shortcut icon', :href => '/images/favicon.ico' }/

    = theme_tags @selected_theme

    = show_widgets @backend_widgets[:head]
  %body{ body_attributes }
    %header#branding= show_widgets @backend_widgets[:branding]

    %nav#main_menu
      = render :partial => 'shared/menu', :locals => { :items => @main_menu_items }

    %section#flash= show_widgets @backend_widgets[:flash]

    %section#content= yield

    = show_widgets @backend_widgets[:tail]
